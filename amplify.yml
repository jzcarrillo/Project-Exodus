version: 1
frontend:
  phases:
    preBuild:
      commands:
        - node --version
        - npm install --legacy-peer-deps
    build:
      commands:
        - npx next build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
