import {sqliteTable,text,integer,index} from 'drizzle-orm/sqlite-core';
export const applications=sqliteTable('applications',{id:text('id').primaryKey(),owner:text('owner').notNull(),service:text('service').notNull(),status:text('status').notNull(),data:text('data').notNull(),created:text('created').notNull(),updated:text('updated').notNull(),version:integer('version').notNull().default(1)},t=>[index('idx_applications_owner').on(t.owner)]);
export const profiles=sqliteTable('profiles',{owner:text('owner').primaryKey(),data:text('data').notNull(),updated:text('updated').notNull()});
export const documents=sqliteTable('documents',{id:text('id').primaryKey(),owner:text('owner').notNull(),application:text('application').notNull(),name:text('name').notNull(),kind:text('kind').notNull(),mime:text('mime').notNull(),size:integer('size').notNull(),created:text('created').notNull()},t=>[index('idx_documents_owner').on(t.owner),index('idx_documents_application').on(t.application)]);
export const activity=sqliteTable('activity',{id:text('id').primaryKey(),owner:text('owner').notNull(),application:text('application').notNull(),action:text('action').notNull(),note:text('note').notNull(),created:text('created').notNull(),seen:integer('seen').notNull().default(0)},t=>[index('idx_activity_owner').on(t.owner)]);
